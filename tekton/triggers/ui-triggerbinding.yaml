apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding

metadata:
  name: ui-triggerbinding

spec:
  params:

    - name: repo-url
      value: $(body.repository.clone_url)

    - name: revision
      value: $(body.ref)

